# CLAUDE.md

Follow `AGENTS.md` for all tasks.

For major UI, UX, or feature work, also consult `NORTHSTAR.md`.

For specific UI work, consult the relevant file in `docs/`.

Do not load every document for every task. Use only the smallest relevant context.
